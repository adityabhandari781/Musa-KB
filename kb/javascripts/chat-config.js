/*
 * Set this to the public URL of the separately deployed chat API, for example:
 *
 * Leave it empty when the API is reverse-proxied at /api/chat on the same host.
 */
window.MUSA_CHAT_API_URL = "https://musa-kb-chat.example.com/api/chat";
// window.MUSA_CHAT_API_URL = "";
